# File logo

Logo aktif saat ini disimpan di `src/asset/branding` dengan nama:

- `logo-umkmku.png` — logo utama aplikasi
- `logo-concern.png` — logo mitra teknologi CONCERN

Semua path logo dikelola dari `src/config/branding.ts`.
Jangan menghapus README ini agar petunjuk penempatan logo tetap tersedia.
